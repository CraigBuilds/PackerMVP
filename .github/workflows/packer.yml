name: build-vm

on:
  workflow_dispatch:

jobs:
  build:
    name: Build VMs
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-system-x86 xorriso

      - name: Setup Packer
        uses: hashicorp/setup-packer@v3
        with:
          version: "1.11.0"

      - name: Fix key permissions
        run: chmod 600 Packer/keys/packer_ed25519

      - name: Initialize and validate Packer (base)
        run: |
          packer init Packer/templates/packer-base.pkr.hcl
          packer validate Packer/templates/packer-base.pkr.hcl

      - name: Build server base VM
        run: packer build Packer/templates/packer-base.pkr.hcl

      - name: Initialize and validate Packer (desktop)
        run: |
          packer init Packer/templates/packer-desktop.pkr.hcl
          packer validate Packer/templates/packer-desktop.pkr.hcl

      - name: Build desktop VM
        run: packer build Packer/templates/packer-desktop.pkr.hcl

      - name: Initialize and validate Packer (optimize)
        run: |
          packer init Packer/templates/packer-optimize.pkr.hcl
          packer validate Packer/templates/packer-optimize.pkr.hcl

      - name: Build optimized VM
        run: packer build Packer/templates/packer-optimize.pkr.hcl

      - name: Upload optimized VM artifact
        uses: actions/upload-artifact@v4
        with:
          name: optimized-vm
          path: build-output-optimize/*

  convert-qemu:
    name: Package QEMU VM
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download optimized VM
        uses: actions/download-artifact@v4
        with:
          name: optimized-vm
          path: vm

      - name: Package QEMU format
        run: |
          mkdir -p dist
          cp vm/craigs_vm dist/craigs_vm.qcow2
          tar -czf dist/craigs_vm_qemu.tar.gz -C dist craigs_vm.qcow2

      - name: Upload QEMU VM
        uses: actions/upload-artifact@v4
        with:
          name: vm-qemu
          path: dist/*.tar.gz

  convert-virtualbox:
    name: Convert to VirtualBox
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download optimized VM
        uses: actions/download-artifact@v4
        with:
          name: optimized-vm
          path: vm

      - name: Install qemu-utils
        run: sudo apt-get update && sudo apt-get install -y qemu-utils

      - name: Convert to VDI format
        run: |
          mkdir -p dist
          qemu-img convert -f qcow2 -O vdi vm/craigs_vm dist/craigs_vm_virtualbox.vdi
          tar -czf dist/craigs_vm_virtualbox.tar.gz -C dist craigs_vm_virtualbox.vdi

      - name: Upload VirtualBox VM
        uses: actions/upload-artifact@v4
        with:
          name: vm-virtualbox
          path: dist/*.tar.gz

  convert-hyperv:
    name: Convert to Hyper-V
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download optimized VM
        uses: actions/download-artifact@v4
        with:
          name: optimized-vm
          path: vm

      - name: Install qemu-utils
        run: sudo apt-get update && sudo apt-get install -y qemu-utils

      - name: Convert to VHDX format
        run: |
          mkdir -p dist
          qemu-img convert -f qcow2 -O vhdx vm/craigs_vm dist/craigs_vm_hyperv.vhdx
          tar -czf dist/craigs_vm_hyperv.tar.gz -C dist craigs_vm_hyperv.vhdx

      - name: Upload Hyper-V VM
        uses: actions/upload-artifact@v4
        with:
          name: vm-hyperv
          path: dist/*.tar.gz

  convert-proxmox:
    name: Package for Proxmox
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download optimized VM
        uses: actions/download-artifact@v4
        with:
          name: optimized-vm
          path: vm

      - name: Package for Proxmox (qcow2 format)
        run: |
          mkdir -p dist
          cp vm/craigs_vm dist/craigs_vm_proxmox.qcow2
          tar -czf dist/craigs_vm_proxmox.tar.gz -C dist craigs_vm_proxmox.qcow2

      - name: Upload Proxmox VM
        uses: actions/upload-artifact@v4
        with:
          name: vm-proxmox
          path: dist/*.tar.gz

  release:
    name: Create Release
    needs: [convert-qemu, convert-virtualbox, convert-hyperv, convert-proxmox]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: vm-*
          merge-multiple: true

      - name: Display artifacts
        run: ls -lh artifacts/

      - name: Create release
        uses: ncipollo/release-action@v1
        with:
          tag: latest
          commit: ${{ github.sha }}
          name: latest
          makeLatest: true
          allowUpdates: true
          replacesArtifacts: true
          artifacts: "artifacts/*"
          body: |
            # VM Images Release
            
            This release contains desktop VM images built with Packer in a three-stage process.
            
            ## Desktop Images
            Built from Ubuntu 22.04 cloud image with XFCE desktop environment in multiple formats:
            - **QEMU** (`craigs_vm_qemu.tar.gz`): QEMU/KVM qcow2 format
            - **VirtualBox** (`craigs_vm_virtualbox.tar.gz`): VirtualBox VDI format
            - **Hyper-V** (`craigs_vm_hyperv.tar.gz`): Microsoft Hyper-V VHDX format
            - **Proxmox** (`craigs_vm_proxmox.tar.gz`): Proxmox qcow2 format
            
            All VMs are based on Ubuntu 22.04 LTS cloud image with cloud-init configuration.
            
            ## Configuration
            
            - Username: `packer`
            - Local login password: `packer` (⚠️ **Change after first login!**)
            - SSH key authentication (password authentication disabled for SSH)
            - SSH public key available in repository
            - 2GB RAM, 2 CPU cores
            
            Extract the archives and import into your preferred virtualization platform.
