# PackerMVP

releases should always use:

```
**Full Changelog**: https://github.com/CraigBuilds/PackerMVP/compare/latest...[new]
```

in the release description.


where `[new]` is the commit for that build 
